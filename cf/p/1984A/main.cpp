#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

typedef long long ll;
const char END = '\n';

void solve()
{
	int n; cin >> n;
	ll a[n]; for (int i = 0; i < n; i++) cin >> a[i];

	
}

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	ll t; cin >> t;

	while (t--)
		solve();
}
