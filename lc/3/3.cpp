#include <vector>
#include <string>
using namespace std;

class Solution {
	public:
		int lengthOfLongestSubstring(string s) {
			vector<string> non_repeating_strings;

			for (int i = 0; i < s.length(); i++) {
				string potential = "";

				for (int j = i; j < s.length(); j++) {
					for (int k = 0; k < s.length(); k++) {

					}
				}
			}
		}
};
